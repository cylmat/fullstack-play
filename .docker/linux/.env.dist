# .env.local
# Copy and rename to file to .env.local and fill in the values for your git credentials

GIT_USER=
GIT_EMAIL=
GIT_USERNAME=
GIT_PASSWORD=gho_
GIT_ASKPASS=/var/www/application/.docker/linux/git-askpass.sh