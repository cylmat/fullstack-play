
# volumes:
#     redis:

services:

    ##################
    ### Key-Values ###
    ##################

    ### REDIS ###

    redis:
        image: redis:8
        container_name: redis
        restart: always
        ports: [63791:6379]
        # volumes:
        #     - ./data/redis/redis.conf:/usr/local/etc/redis/redis.conf:ro
